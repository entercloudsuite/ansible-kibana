---
galaxy_info:
  author: Federico Minzoni
  description: Playbook for Elasticsearch.
  company: Enter srl
  license: MIT
  min_ansible_version: 2.4
  platforms:
    - name: Ubuntu
      versions:
        - xenial
  galaxy_tags:
    - molecule
    - elasticsearch
    - search-engine
    - logs

dependencies:
  - { role: elastic.elasticsearch, version: 5.5.1 }
  - { role: entercloudsuite.prometheus-exporter }
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
